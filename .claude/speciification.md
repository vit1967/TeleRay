###ToDo:
1. Наблюдаю что Если в момент, когда я запуская TeleRay, в уже ранее запущеом v2rayTg выставлен текущим нерабочий ключ, но впн (кн.Run) выключен ,
то после старта TeleRay и запуска таймерного авто-режима в 1-м же цикле TeleRay заставляет v2rayTg найти лучший ключ , переключиться на него и включить ВПН. А если в момент запуска  TeleRay в v2rayTg тоже текущим выставлен нерабочий кл., но кн.Run нажата (ВПН включен), то этого не происходит. Проскакивает ввроде бы Toast "timeout..." ,и TeleRay мн. циклов пытается скачать прокси.

2. TeleRay часто шлет в v2rayTg одни и те же ключи-профили ( видимо дублирующиеся запросы в разн.полях requestRow[i]. М.б. предупреждающий Toast выводить пря явном дублировании ранее уже вбитых вариантов? , при повт. запросе у бота, или из резервов) и "заспамливает" базу ключей v2rayTg.
Проще всего с этим бороться так по м.: в v2rayTg есть п.меню "удалить дубликаты профилей групп".  
измени / дополни @v2RayTgTeleRayAPIspecification.md - API используемого IPC-обмена, и AutoConnectController.java чтобы в каждом цикле, сразу после передачи кл. загрузки 
3. Сделай в панели ConfigSubscrBotRequest.java ниже поля подписок и поля "кол-во мин. между одинак.запрос." кнопту теста "подбор в v2rayTg лучшего кл.", из которой польз-ль может протестировать ф-ю в AutoConnectController.java, которая по IPC заставляет v2rayTg подобрать лучший кл.-профили , выключить ВПН, перейти на лучший кл. и включить ВПН(эмулируя нажатие Run)


##  Поругай непонятные и противоречивые п. этого toDo (исправлю), покажи план. потом реализуй.

При непонятках, сомнениях и отходе от задач ToDo сначала останавливайся и уточняй. не стесняйся задавать уточняющие вопросы!

по ADB подключен realme RMX3581 , и TeleRay запущен (говори когда что тапнуть -это я могу ;).


fix build errors








##old ToDo orders 9 ( Выполнено полностью. Просто к сведению ) ##
Запущеные 1 за др. v2rayTg и TeleRay работают и взаимодействуют как надо ,даже при изначальном полн. отсутствия ключей и прокси, 
    после включеиия циклов авто-режима успешно находят включают сначала прокси, а потом ключи в v2rayTg.
НО наблюдаю следующее. Если во вр. работы TeleRay обновить /перезапустить приложение v2rayTg, то TeleRay не только теряет коннект, но и не может его восстановить в последующих циклах авто-режима (несмотря на то, что на панели обновленного v2rayTg уже видны рабочие ключи), как будто он не видит v2rayTg.
Не может ли это быть из за потери соединения IPC при пере-загрузке (выгрузке загрузке) 2го приложения? Тогда включи в цикл (в ф-ю коннекта с v2rayTg via IPC ) поиск (повторный ) вновь запущенного 2го приложения v2rayTg и пере-подключение.    






##old ToDo orders 8 ( Выполнено полностью. Просто к сведению ) ##
1. Приложения TeleRay & v2rayTg в целом работают ожидаемо. Но  
Наблюдаю при их взаимодействии следующее:
    1.1. в v2rayTg после уже 1-го минутногоо таймер-цикла успешно импортированы много ключей , среди которых, как потом я проверил, был и 1 рабочий, но ,тем не менее выставление v2ray на этот рабочий ключ и включение ВПН командами через IPC не происходило много таймерных циклов подряд. Я так и не увидел визуально на экране v2rayTg перемещения белой полоски, показывающей текущий используемый ключ с нерабочего ключа, на рабочий, хотя (овтряю) через неск. циклов, подгрузив еще мн. ключей from TeleRay, v2ray включила ВПН , и TeleRay после этого ожидаемо перестал "мучить" глючные прокси, отключил их и подключился через ВПН(v2rayTg). 
Возможно это можно было сделать раньше (надо проверить). Возможно надо сразу после хоть какого-то подключения к серверам телеграм через MTProxy добиться сначала загрузки в v2rayTg не 1го, а мин. пары проверенно-РАБОЧИХ ключей, и только после этого уже заставлять v2ray перейти (и выставить полоску на экране списка!) на РАБОЧИЙ ключ, и только после этого отключать все прокси  и переходить на ВПН(v2rayTg).

2. Несм. на то, что в панели общей настройки подписок SubscriptionsConfigFragment у меня выставлено "Мин.рабочих MTProxy" 6 шт.
я за много минутных тамерных циклов ни разу так и не видел в listProxy -общем списке Proxy Telegram более 2х прокси.
Возм. резервируется мало прокси.
Надо увеличить (снять ограничения на добавления РАБОЧИХ прокси, если есть где) , чтобы кол-во РАБОЧИХ прокси в дlistProxy было =  "Мин.рабочих MTProxy" .

3. Оказ. есть еще боты, раздающие прокси. , поэтому надо:
        3.1. дополнить окно-селектор выбора showAddSubscriptionDialog еще  п. "MTProxy Bot"? при выборе которого откроется ...
        3.2. ..новая созданная тобой класс-панель ConfigMtpBotRequest настроек подписки на бота, отдающего  MTProxy. 
            Это м.быть гибрид уже имеющихся:
            класса-панелеи подписки на бота, отдающего профили(vless,vmess,trojan) ConfigSubscrBotRequest и 
            класса-панелеи подписки на каналы MTProxy ConfigMTProxySubscr.
            Т.е. из ConfigSubscrBotRequest в ConfigMtpBotRequest берется аналогичный выбор подписки на бота с помощью DialogsActivity,  
            а из ConfigMTProxySubscr - аналогичная работа полей для MTProxy, т.е. 2 массива ( , i-ые элементы которых парно
            выводятся в 1й позиции  1 под другим. Сверху  поле с текстом MTProxy proxyRow[i] , аналогичное requestRow[i] in ConfigMTProxySubscr , 
            ниже поле с рез-тами его тестов testProxyRow[i] , работающее аналогично updateKeyRow[i]
            (все работает как с полями   requestRow[i], updateKeyRow[i] in ConfigMTProxySubscr ) 
        3.3. в SubscriptionsConfigFragment и во всех ф-ях для работы с MTProxy , добавить и учиесть также работы с  прокси, добавляемых в панели настроек ConfigMtpBotRequest в составе остальных прокси.

##  Поругай непонятные и противоречивые п. этого toDo (исправлю), покажи план. потом реализуй.

При непонятках, сомнениях и отходе от задач ToDo сначала останавливайся и уточняй. не стесняйся задавать уточняющие вопросы!

fix build errors





##old ToDo orders 7 ( Выполнено полностью. Просто к сведению ) ##

in java/org/telegram/ui/SubscriptionsConfigFragment.java

1. добавь ниже кнопки "+ Add Subscription"
выше кн. "Turn Auto upd. v2ray & MTP"
(т.е. между этими кн.)
редактируемое цифровое поле "мин. кол-во рабочих кл.-профилей" minWrkKeys
, редактируемое цифровое поле  "мин. кол-во рабочих MTProxy" minWrkMTProxy
, редактируемое цифровое поле период проверки мин. timeTstMinute (по дефолту 5 )

2.Создай ф-ю обработки нажатия кн. "Turn Auto upd. v2ray & MTP" -  startLoopTstConnect(...)
Эта ф-я   2.1. запускает таймер  и 2.2. еще 1ну ф-ю testAllConnections(..)    
    каждый раз , когда таймер, вычитающий посекундно значение в минутах , заданное в  timeTstMinute в сек. (* 60 сек  ), доходит до 0,
            должен авт. запускаться новый цикл ожидания "обнуления таймера" , и ф-я  testAllConnections        
       Также при 1вом нажатии меняется название кнопки с "Turn Auto upd. v2ray & MTP" на "STOP WORKING Auto v2ray & MTP"(при повт.нажатии на "STOP WORKING Auto v2ray & MTP" таймер выключается ).  
3. ф-я testAllConnections(..)  :
    3.1.должна иметь доступ к глобальному списку proxy Telegram, всем спискам 
    3.2. а также к ф-ции (ф-ям), осуществляющим взаимодействие (обмен данными = командами и ключами ) 
        с v2rayTg  согласно спецификации  @v2RayTgTeleRayAPIspecification.md.
    3.3. ф-я testAllConnections снач. проверяет наличие v2rayTg и взаимодействие с ней о IPC . При отсутствии этого -Toast(диагностика)
        3.3.1. testAllConnections заставляет v2rayTg проверить все включенные ключи на ping  и включить ключ с мин. ping (но не ru)
        3.3.2.   если v2rayTg удается восстановить / поддерживать норм. соединение с зарубежным интернет (типовые ping проверки), 
            то отключаются используемые proxy из глобального proxyList Telegram,  
                        и переход к фазе "резервирования" п. 3.6.   
        3.4. если v2rayTg НЕ удается восстановить / поддерживать норм. соединение с зарубежным интернет (типовые ping проверки),
                то Toast("v2ray работает, но не может восстановить соединения" ),и ..-> 3.4.1.
            3.4.1.к фазе "MTProxyTests",т.е. запускается последовательное тестирование всех proxy из глобального proxyList Telegram.
                если находится рабочий proxy (с приемлиемым time of ping), то Telegram включается на работу через него, и и переход к фазе "резервирования" п. 3.6.  
            3.5.
            3.6. фаза "резервирования" : 
                3.6.1. ф-я проверяет все подписки на MTProxy , сохраненные ранее пользователем в ConfigMTProxySubscr, 
                    и соответствующие каналы подписок на приход в них новых сообщений (содержащх в т.ч. прокси), еще ранее не   
                обработанных(не   добавленных  в  ConfigMTProxySubscr в соответствующие поля updateKeyRow[i]. 
                    При наличии таковых и удачном извлечении из них прокси (например ф-ямии из ConfigMTProxySubscr) , 
                    эти прокси сразу добавляются в глобальный proxyList Telegram
                    до достижения кол-ва прверенно рабочих прокси не меньше, чем minWrkMTProxy
                3.6.2. ф-я последовательно, с задержкой в 4 сек. , перебирает в ConfigSubscrBotRequest все команды ботам  requestRow[i] , посылает их , принимая обновления ключей от ботов ,и обновляя ключи в updateKeyRow[i], а также посылая их v2rayTg (по  @v2RayTgTeleRayAPIspecification.md) ДО достижения кол-ва прверенно рабочих в v2rayTg ключей не меньше, чем minWrkKeys

 П. 3.5 - пропусти.      
П. 3.3.1 — б) просто слать CMD_TEST_KEYS и доверять v2rayNG. Главное удостовериться что можно обойтись без MTProxy и отключить их.         
  П. 3.3.2 / 3.4  - нужна отдельная проверка доступности основных необходимых серверов Telegram                                            
 П. 3.6.1 : откуда брать "новые необработанные" — из НОВЫХ последних сообщений канала, которые до этого запуска ф-ции testAllConnections   
еще не были считанны(пользователем вручную) в уже имеющиеся в ConfigMTProxySubscr поля updateKeyRow[i] (не более 20ти последних сообщений  
канала ).                                                                                                                                  
                                                                                                                                           
  П. 3.6.2 — посмотри как реализовано в ConfigSubscrBotRequest , с теми же логикой и таймаутами.     Там по тапу пользователем на updateKeyRow[i] обновленные ключи отлично получаются у бота и отправляется v2rayTg.                   



##  Поругай непонятные и противоречивые п. этого toDo (исправлю), покажи план. потом реализуй.

При непонятках, сомнениях и отходе от задач этого ToDo сначала останавливайся и уточняй. не стесняйся задавать уточняющие вопросы!

fix build errors







##old ToDo orders 6 ( Выполнено полностью. Просто к сведению ) ##
испрравить недоделки, допущенные др.LLM(cli) при выполнении следующих задач (повторяю промпт ей, хотя в целом он на 80% выполнен и код работает, но НЕ реализована вся логика
Это -исходный проект (до неудачной переделки).
неудачная(частично удачная)  переделка ЭТОГО проекта - @link to TeleRay-IU03

## Изначально были поставлены след. задачи(сейчас уточненные, но если непонятно, не стесняйся спросить уточнить ):
##ToDo
1. Создать новую панель управления подписками MTProxu ConfigMTProxySubscr , взяв за образец "панель управления подписками на ключи V2ray" - ConfigSubscrBotRequest.java.  Эта панель должна открываться для заполнения свеже-созданной пустой(пока еще) подписки на еще 1 channel MTProxy также, и как альтернатива (после окошка выбора меж ними) открытию панели ConfigSubscrBotRequest = "конфигурации подписки на бот v2ray".
1.1. в ConfigMTProxySubscr сохраняется поле 
  // Subscription section
        subscriptionRow = rowCount++; //тут ccskrf=название channel , чат с которым выбран 


    1.1.1.В новой панели  ConfigMTProxySubscr по короткому тапу на subscriptionRow открывается окно выбора канала подписки ChannelsActivity (аналогичного  DialogsActivity , но с фильтром для выбора только из каналов и записи ссылки канала в поле subscriptionRow. Разница еще в том ,что в при заполнении поля subscriptionRow возвращаемыой из DialogsActivity ссылкой на channel не надо символ "@" к началу добавлять )
    1.1.2. по длинному тапу на subscriptionRow - всплывающее меню "удалить"(удаляет очищает всю панель-подписку" / "перевыбрать" .

        // Request section - dynamic based on v2rayRequestCount
        for (int i = 0; i < SharedConfig.v2rayRequestCount; i++) {
            requestRow[i] = rowCount++;  // в это динамически добавляемое поле пользователь выбирает с помощью тоже окна ChatActivity 1 из сообщений канала , содержащее данные MTProxy 
            updateKeyRow[i] = rowCount++;  // здесь будет результат проверки прокси 
        }
        
        // Add button row (always shown if count < 14)
        if (SharedConfig.v2rayRequestCount < 14) {
            addButtonRow = rowCount++;
        } else {
            addButtonRow = -1;
        }

    1.1.3.убрать ф-ю sendToV2Ray, взаимодействием с V2Ray. и все, связанное с посылками команд боту (это ж channel , а не бот), 

    1.2. По короткому тапу на  requestRow[i] открывается ChatActivity для выбора сообщения из канала вместо выбора команд боту в чате. 
        1.2.1. ChatActivity открывает чат канала, указанного в subscriptionRow.  (например в Bundle аргументов, передаваемых при создании ChatActivity, вместо ключа user_id (используемого для приватных чатов с пользователями или ботами) нужно передать ключ chat_id с идентификатором канала. ) 
    1.2.3. прокси-сервер выделяется из сообщения канала и записывается соответствующее поле updateKeyRow[i]
    1.2.4. при длинном тапе на requestRow[i] всплывает меню "удалить" / "удалить и выбрать другой" 


    1.3. По тапе на поле updateKeyRow[i], в корором изначально, до тестирования вписывается значение "тестируется",
    1.3.1. прокси из соотетствующего ему поля requestRow[i] (в которое изначально при создании пишется "выбери прокси из канала")   извлекается, берутся данные  прокси-сервера(при необходимости), тестируется (с пом. через имеющейся в Telegram ф-ции ConnectionsManager.checkProxy(...), запись в поле updateKeyRow[i] после тестирования меняется либо на время пинга по рез-ту тестирования ,либо на "недоступен" .
     1.3.2. После тестирования (при усешном) прокси добавляется в центральный proxyList (в  Telegram app.  есть например `SharedConfig.addProxy(proxy)` )


3. При тапе на subscriptionRows открывается окно-селектор (либо на v2ray - ConfigSubscrBotRequest, либо на MTProxu - ConfigMTProxySubscr), после которого открывается соответствующее пустоая панель из еще не заполненных, свеже-созданных новых полей-записей (либо  ConfigSubscrBotRequest, либо ConfigMTProxySubscr)



4. реализовать динамически общий список всех подписок SubscriptionsConfigFragment.java
4.1. Структура данных и хранение
•Массив в коде: Вместо private int subscription1Row, необходимо использовать ArrayList<Integer> subscriptionRows = new ArrayList<>().
•Хранение: Поскольку SharedConfig сейчас хранит только одну строку v2raySubscription1, вам потребуется изменить логику сохранения. Оптимально использовать JSONArray для сохранения списка имен ботов в одну строку настроек или добавить методы для сохранения/загрузки списка.
•Типы подписок: Для хранения информации о том, является ли подписка ConfigSubscrBotRequest (v2ray) или ConfigMTProxySubscr (MTProto), стоит создать простую модель данных:
class SubscriptionItem {
    String botUsername;
    int type; // 0 для V2Ray, 1 для MTProto
}

4.2. Принцип реализации SubscriptionsConfigFragment - Динамическое управление строками (UI)


•Метод updateRows(): В этом методе вы будете очищать subscriptionRows, проходить циклом по вашему списку данных и добавлять индексы строк.
•Кнопка «+»:  в конец списка секцию с TextSettingsCell или специальной кнопкой, при нажатии на которую в список данных добавляется новый пустой элемент, вызывается updateRows() и listAdapter.notifyDataSetChanged().
    4.3. создать в SubscriptionsConfigFragment кнопку "+ подписка" внизу SubscriptionsConfigFragment , но выше существующей кнопки "SENT KEYS TO V2Ray", и но под полями добавления новых подписок. Этой кнопкой и будет открываться панель настройки новой подписки (либо на v2ray ConfigSubscrBotRequest, либо на MTProxu ConfigMTProxySubscr.

Процесс выбора типа подписки и возврата значения
Селектор типа (Окошко выбора)
Использование промежуточного окна выбора (через AlertDialog или BottomSheet) — это очень удобное и стандартное решение для Telegram-клиентов.
•Логика: При тапе на пустую позицию subscriptionRows[N], вы показываете AlertDialog с двумя вариантами: "V2Ray Бот" и "MTProto channel".
•Переход: В зависимости от выбора, вы открываете либо ConfigSubscrBotRequest, либо ConfigMTProxySubscr.

5. Возврат значения напрямую
Чтобы значение возвращалось напрямую без лишних окон:
5.1.Интерфейс обратного вызова (Delegate): Передайте в открываемый фрагмент объект-слушатель (например, ConfigSubscrBotRequestDelegate), в котором будет метод didSelectBot(String username).
5.2.Результат: Когда в дочернем фрагменте выбран бот, он вызывает метод делегата и закрывается (finishFragment()).
5.3.Обновление: SubscriptionsConfigFragment, получив значение через делегат, записывает его в нужную позицию массива и обновляет список. Это исключает необходимость в дополнительных подтверждениях.
Резюме по удобству
Такой подход (Массив + Селектор типа + Прямой возврат) сделает интерфейс гибким:
•Массив [10] позволит пользователю управлять несколькими источниками конфигураций.
•Промежуточный селектор четко разделяет логику разных протоколов (v2ray vs MTProto), не перегружая основной экран настройками, которые могут не понадобиться.
• Прямая запись имени бота при возврате обеспечит «бесшовный» пользовательский опыт, так как пользователь уже выполнил целевое действие во вложенном экране.
Примечание: На данный момент ConfigMTProxySubscr отсутствует в проекте, его нужно будет создать по аналогии с ConfigSubscrBotRequest.


собрал-запустил 
## По результату наблюдаются следующие замечания (в проекте @TeleRay-IU03/TMessagesProj ):

10.1. в SubscriptionsConfigFragment (ранее V2rayConfigFragment ) при тапе  на subscriptionRows открывается окно-селектор (либо на v2ray - ConfigSubscrBotRequest, либо на MTProxu - ConfigMTProxySubscr), после которого открывается соответствующее пустоая панель из еще не заполненных, свеже-созданных новых полей-записей (либо  ConfigSubscrBotRequest, либо ConfigMTProxySubscr) выборе в окно-селектоор-е верхнего п. "V2ray Subscription" должна открываться "пустая" панель ConfigSubscrBotRequest 
А наблюдаю, что появляться новая пустая строка @V2ray Subscription" !


10.2. При выборе в окно-селектоор-е нижнего п."MTProxy Subscription" открывается видимо ChatActivity по идее для выбора канала с MTProxy, но поч-то не видно ни 1-го канала, а если их специально найти в поиске и указать, выскакивает окно:

"Send message
Sorry, you can't send messages to this channel. 
                                    OK  "

10.4. В SubscriptionsConfigFragment при тапе на 

    Видимо всеже для выыбора канала сделай отдельный класс-окно ChannelsActivity (похожий на DialogsActivity)
    10.4.1.по тапу на subscriptionRow открывается окно выбора канала подписки ChannelsActivity (аналогичного  DialogsActivity , но с фильтром для выбора только из каналов ии записи ссылки канала в поле subscriptionRow. Разница еще в том ,что в при заполнении поля subscriptionRow возвращаемыой из DialogsActivity ссылкой на channel не надо символ "@" к началу добавлять )
    10.4.2. по длинному тапу на subscriptionRow - всплывающее меню "удалить"(удаляет очищает всю панель-подписку" / "перевыбрать" .

# Кнопки Кнопка "+ Add Subscription", как и " SEND KEYS TO V2RAY "(котрую кст. надо переименовать в "Turn Auto upd. v2ray & MTProxy")  внизу @link to TeleRay-IU03/TMessagesProj/src/main/java/org/telegram/ui/SubscriptionsConfigFragment.java - ОТЛИЧНЫЕ получились :) Надо все подобные кн. "+...."  сделать аналогичными по дизайну )



сверься еще раз с   ToDo, Поругай непонятные и противоречивые п. (исправлю), покажи план. потом реализуй.


При ЛЮБЫХ непонятках, сомнениях и отходе от задач этого ToDo сначала останавливайся и уточняй - задавай уточняющие вопросы!!!

fix build errors














##old ToDo orders 5 ( Выполнено полностью. Просто к сведению ) ##
Твоя задача испрравить ошибки и недоделки, допущенные др.LLM(cli) при выполнении следующих задач (повторяю промпт ей, хотя в целом он на 80% выполнен и код работает, но НЕ реализована вся логика, работавшая в предыдущем коде. Предыдущий код доступен для ознакомления в проекте по symlink @linkTeleRay-IU02old , в частн. переделывалась панель @linkTeleRay-IU02old/TMessagesProj/src/main/java/org/telegram/ui/ConfigSubscrBotRequest.java ):

         Переделай панель ConfigSubscrBotRequest.java так:
        1. В Request section, вместо 4ех однотипных 2ух строчных позиций
                // Request section
                request1Row = rowCount++;
                updateKey1Row = rowCount++;

                request2Row = rowCount++;
                updateKey2Row = rowCount++;

                request3Row = rowCount++;
                updateKey3Row = rowCount++;

                request4Row = rowCount++;
                updateKey4Row = rowCount++;
        сделай массив однотипных и одинаково функциональных "позиций" до макс. 14ти элементов.
        2. Сначала (по умолчанию) видна 1на 1вая позиция 
                requestRow[0] = rowCount++;
                updateKeyRow[0] = rowCount++;
            которая выполняет полностью весь функционал старой позиции 
                request1Row = rowCount++;
                updateKey1Row = rowCount++;
            , включая (просто напоминаю на всякий, т.к. функционал этих старых полей
                request1Row = rowCount++;
                updateKey1Row = rowCount++;

                request2Row = rowCount++;
                updateKey2Row = rowCount++;

                request3Row = rowCount++;
                updateKey3Row = rowCount++;

                request4Row = rowCount++;
                updateKey4Row = rowCount++;
            реализован хорошо, и не хотелось бы , чтобы он изменился при переходе на 
                requestRow[0] = rowCount++;
                updateKeyRow[0] = rowCount++; и.т.д.
                то есть (примерно напоминаю): 
                2.1. При тапе ( коротком ) на поле request1Row "Запрос 1" :
                  Если поле requestRow[N] чем-то заполнено, и в конце заполняющей строки  нет символа ";"(точка с запятой) то поле доступно только в режиме обычного ручного текстового редактирования пользователем.
                 2.2. Если поле requestRow[N] пусто, или  заполнено, но в конце заполняющей строки - символ ";"(точка с запятой)  то  должно открывается окно ChatActivity переписки с ботом . Имя бота для ВСЕХ позиций берется  из ранее заполненного поля `subscription1Row`(выбором чата DialogsActivity). В subscription1Row ничего менять не надо (получается)!
                 2.3.  При возврате из ChatActivity  происходит автозаполнение поля requestRow[N]  из буфера обмена (всеми собранными в 1ну строку текстами, выбранным в окне ChatActivity переписки с ботом  ) НО если поле requestRow[N] было не пусто, а заполнено, но в конце заполняющей строки - символ ";"(точка с запятой) , то из буфера производится дописывание после последнего символа ";"(точка с запятой), и добавленного за ним " "(пробела)
                2.4. (не реализованный update. работающий в старом коде. возм. пот.что я забыл его тут повторить в промпте для той LLM) При тапе на updateKeyRow[N] отправ :
                    2.4.1. Если поле updateKeyRow[N] еще не заполненное иполученный 

3. после всех, уже заполненных (ранее) и видимых позиций , ниже должна постоянно (до полного заполнения массива 14ю значениями) 
            находится кнопка "+ Add Subscr.Bot command"  подобно тому, как  в  TMessagesProj/src/main/java/org/telegram/ui/FiltersSetupActivity.java  
    при тапе - наажатии на "+ Create New Folder" (создания новой папки) открывается  файл FilterCreateActivity.java   из FiltersSetupActivity.java,
         и потом добавленные папки появляются внизу этой панели "отодвигая" кнопку "+ Create New Folder" еще ниже (возьми как образец).
         
        В остальном функциональность ConfigSubscrBotRequest (самое главное нормально работающая сейчас передача ключей-профилей to app. v2rayTg in module v2ray при тапе на заполненные updateKeyRow[N] )




4. Итак, по исполнении этих задач реализован массив "2х-строчных позиций" и автоматически сдвигаемая ниже кнопка "+ Add Subscr.Bot command" за ним. 
Но в предыдущием коде без массива "позиций" @linkTeleRay-IU02old/TMessagesProj/src/main/java/org/telegram/ui/ConfigSubscrBotRequest.java 
было так:
    4.1.В поле updateKey1Row - сначала надпись "Запрос N ключа"(красным), После 1го тапа на это поле команда боту(взятая из поля request1Row ) посылается в чат. В это время в поле updateKey1Row надпись "ожидаю"(красным). После получения ключа(ей) и его символьной проверки ключ(и) записывается в updateKey1Row а также передается ключа в ф-ю sendToV2Ray , которая посылает эти ключ(и) отдельно паралельно работающему приложению v2rayTg (модуль v2ray ) по IPC согласно спеки  @v2RayTgTeleRayAPIspecification.md . А если ключ не приходит или плохой(не прошел символьную проверку), то в поле пишется "Запрос N ключа повторно"(красным)
    Это не было реализовано.  При тапе на поля updateKeyRow[N] в новом коде они не обновляются (не заполняются ключами от бота), и ,соответвенно ничего не посылается в паралельно запущенный и работающий v2rayTg , и не пишется в ее таблицу, хотя в предыдущем коде все это работало )
При реализации не забудь учесть замену 4х позиций в старом коде
                request1Row = rowCount++;
                updateKey1Row = rowCount++;

                request2Row = rowCount++;
                updateKey2Row = rowCount++;

                request3Row = rowCount++;
                updateKey3Row = rowCount++;

                request4Row = rowCount++;
                updateKey4Row = rowCount++;
    на варьируемое кол-во позиций , хранимых не в отд. переменных, как раньше, а в массиве(вах) до 14 позиций макс. в новом коде.

4.2. При ДОЛГОМ тапе на текст ключа(-чей) в updateKeyRow[N] пусть поле с ключем(ями) переходит либо в режим ручного редактирования либо (на тв. усмотрение, как проще) в режим со всплытием меню "удалить / запросить-обнвить / показать_в_отд.окне / отправить->v2rayTg " для возможности очистки поля / диагностики.

5. При нажатии на  "+ Add Subscr.Bot command" формируется новая "позиция" с соответствующими аналогичными исходными надписями в полях, но с новыми индексами (+1) , и сразу 1 раз вызывается ChatActivity для заполнения поля requestRow[N] (аналогично поведению старого кода с позициями в переменных без массивов)
5.1. Мне еще кажется что предлагаемая за образец кнопка "+ Create New Folder" в  TMessagesProj/src/main/java/org/telegram/ui/FiltersSetupActivity.java  
 лучше выглядит, чем кнопка "+ Add Subscr.Bot command"  ;)

6.Проверь еще раз весь ли функционал из старого кода без массивов позиций реализован в новом коде с массивами позиций.



7. Брать ли в кач-ве исходного, для "доделки без ошибок", предыдущий код из @linkTeleRay-IU02old или этот, уже частично переделанный (но с ошибками), -на твое усмотрение.


8.При ЛЮБЫХ непонятках, сомнениях и отходе от задач speciification.md сначала останавливайся и не стесняйся уточнять задавая уточняющие вопросы!

Сейчас для отладки (LogCat) по ADB подключено realmr RMX3581 (скажи тогда в какой момент (перед запуском тобой ADB и обоих v2ray_standalone и TMessagesProj_App приложений для отладки) мне проверить подключение, и потом тапнуть по тексту принятого от бота ключа в ConfigSubscrBotRequest для отправки его v2rayTg)

9. fix build errors после всех больших изменений и в конце.
















##old ToDo orders 4 ( Выполнено полностью. Просто к сведению ) ##
Оба приложения запускаются и работают отдельно (сначала v2rayTg = v2ray_standalone Run/Debug conf. , after TeleRay = TMessagesProj_App Run/Debug conf.), и успешно взаимодействуют др. с др. через IPC по спеке @TgXRayAPIspecification.md , то есть пока вручную,
по тапе(клике)  по ключу в TMessagesProj_App  в панели ConfigSubscrBotRequest.java , и успешной отработки ф-ции sendToV2Ray , о чем говорит Toast "Sending key to V2Ray", и уже в паралельно запущенном app. v2ray_standalone  появляется сначала Toast "Received V2Ray profile from bot".
1. 1я проблема в том, что в v2ray записывается  только 1-й ключ-профиль из 3х ключей, изначально отдаваемых ботом ConfigSubscrBotRequest в ответ на его посылку - запрос команды боту.
Текст , отдаваемый ботом содержит лишние строки, подлежащие фильтраци в TeleRay , и выглядит например так:
"[3/9/26 8:20 PM] в волков: 🎏 FI2
[3/9/26 8:20 PM] HLVPN 🤖: ⭐ Ключи Undef ⭐
            
   ShadowSocks:
временно отсутствует
    
   Trojan:
trojan://JUysn2DXjAeHM8yGBM609IHum@undef-fin-31b.undef.network:443?sni=undef-fin-31b.undef.network&host=undef-fin-31b.undef.network&security=tls&type=ws&path=%2Ff2fc2a1f#HLVPN_03-09FIN_2_UNDEF

   Vmess:
vmess:

   Vless:
vless://84e6c089-2f0a-35e6-9309-49b84b807d88@undef-fin-31b.undef.network:443?encryption=none&flow=xtls-rprx-vision&security=reality&sni=m.vk.ru&fp=chrome&pbk=6z3VlbF2v0C3FTWQQemYDxX-MIzwaz_k8_B-xyc0Xyg&sid=9b5ffd5a680dd823&type=tcp#HLVPN_03-09FIN_2_UNDEF

(Нажми на текст ключа, чтобы скопировать. Не отправляй его никому!)

Новый ключ от этой страны будет доступен через 6 час. 0 мин."
1.1. проверь(измени) ф-цию приемки-от бота и фильтрации в TeleRay , чтобы в поле ключа записывалось все 3 ключа, разделенные пустыми строками др. от др., но без лишних строк.
1.2. улучши ф-ю приема посылки IPC от TeleRay к V2rayTg, чтобы успешно импортировалось все 3 ключа 



2. По возможности предусмотри (подготовь) для этой же цели внешнее управление получением ключей по подписке и управление v2rayTg  встроенным AI через MCP-модуль в обоих приложениях, которое будет реализовано позже (через подключаемые динамические библиотеки .dex ), и подробно допиши это (MCP , API интерфейсы ) в конец CLAUDE.md

При ЛЮБЫХ непонятках, сомнениях и отходе от задач этого ToDo сначала останавливайся и уточняй - задавай уточняющие вопросы!!!

fix build errors






##old ToDo orders 3 ( Выполнено полностью. Просто к сведению ) ##
1. В V2rayConfigFragment.java : При тапе на subscription1Row всегда открывается ConfigSubscrBotRequest.java.
2. Если в V2rayConfigFragmentпри при тапе  на subscription1Row оно было пусто, то сразу после открытия ConfigSubscrBotRequest
    открывается ChatActivity для выбора чата с ботом  
    (ранее это происходилов в V2rayConfigFragment и было реализовано примерно так:
            } else if (position == subscription1Row ) {
                wasDialogSelected = false;
                choosingForSubscription = position == subscription1Row ? 1 : 2;
                Bundle args = new Bundle();
                args.putBoolean("onlySelect", true);
                args.putInt("dialogsType", DialogsActivity.DIALOGS_TYPE_DEFAULT);
                DialogsActivity dialogsActivity = new DialogsActivity(args);
                dialogsActivity.setDelegate(V2rayConfigFragment.this);
                presentFragment(dialogsActivity);
            }
    -реализуй аналогичное поведение теперь в ConfigSubscrBotRequest. Т.е. 
    Если request1Row пустое → открыть окно выбора чата (DialogsActivity) , из которого затем вернуть Username бота выбранного чата 
    и записать  в request1Row (с "@" в 1-вом символе )
  - Если выбор из DialogsActivity пустой (польз-ль нажал стрелку "назад" не выбрав чат), то поле subscription1Row очищается 
        ( и возм. Обновляет UI через listAdapter.notifyDataSetChanged() )




##old ToDo orders 2 ( выполнено  ) ##
    Краткое содержание плана:

    Проблема
     - Дублирование строк "Подписка 2" (видно на фото)
     - Буфер обмена не записывается в request1Row после возврата из ChatActivity
     - Конфликт переменных choosingForSubscription и choosingForRequestField

    Решение
     1. Удалить Подписку 2 из V2rayConfigFragment
     2. Создать новый фрагмент ConfigSubscrBotRequest.java для настройки Подписки 1
     3. Оставить одно кликабельное поле "Подписка 1" в главном окне

    Архитектура

     1 V2rayConfigFragment (главный экран)
     2   └─→ Подписка 1 (тап) → ConfigSubscrBotRequest
     3        └─→ subscription (тап) → DialogsActivity (выбор бота)
     4        └─→ request1/11/12/13 (тап) → ChatActivity (копирование команды)
     5        └─→ updateKey1 (тап) → отправка сообщения боту

    7 шагов реализации:
     1. Создать ConfigSubscrBotRequest.java
     2. Модифицировать V2rayConfigFragment.java (удалить subscription 2 со всеми связанными полями  request21Row,    request22Row,        request23Row ,        updateKey2Row ,        key2Row ) 
     3. В ConfigSubscrBotRequest особое внимание обратить на логику автозаполнения из буфера после вызова ChatActivity из полей 
    request1Row ,
        request11Row 
        request12Row 
        request13Row 
ЭТИ Поля в ConfigSubscrBotRequest должны работать так:
 При тапе ( коротком ) на поле request1Row "Запрос 1" :
 1.1. Если поле request1Row чем-то заполнено, то поле доступно  в режим текстового редактирования поля request1Row вручную пользователем.
 1.2. Если поле request1Row пусто, то  должно открывается окно ChatActivity переписки с ботом (А НЕ окно выбора чата DialogsActivity) . Имя бота беретс  из ранее заполненного поля `subscription1Row`.

-1.3.  При возврате из ChatActivity  происходит автозаполнение поля request1Row  из буфера обмена (выбранным в окне ChatActivity переписки с ботом  ) 
1.4. Если стереть вручную путем редактирования поле request1Row , затем выйти из панели настроек ,и потом зайти обратно , то поскольку поле request1Row пусто, то при тапе на него должно открывается окно ChatActivity переписки с ботом (по п. 1.2.)   

1.5. При тапе на уже ранее заполненное поле request1Row ничего открывать не надо, а пользователю должны быть доступны все стандартные ф-ции редактирования текстовых редактируемых полей (с стандартным всплывающим при долгом тапе мень "выделить, выбрать все, удалить ")
1.6. редактируемое поле request1Row должно находиться в 1й строке (на 1м вертикальном уровне ) с заголовком "Запрос 1", но правее его (не загораживая заголовок собой)


     4. Исправить Focus Change Listener
     5. Обновить SharedConfig.java
     6. Обновить strings.xml
     7. Исправить адаптер

    Критерии успеха:
    ✅ Нет дублирования строк
    ✅ Буфер обмена работает корректно
    ✅ Все поля запросов работают независимо
    ✅ Сборка проходит без ошибок



##old ToDo orders 1  ( выполнено  )  ##
размножь request1Row в поля 
 request11Row ,  
 request12Row , 
 request13Row ,  
 
При исп- нии всех этих запросов полученный от бота ключ попадает в 1 старое поле key1Row
, и после поля subscription2Row аналогично поля , аналогичные старому request2Row
 request21Row ,  
 request22Row ,  
 request23Row ,  
При исп- нии всех этих запросов полученный от бота ключ попадает в 1 старое поле key2Row




##old ToDo orders 0  ( выполнено  )  ##

В панели настроек-тестов взаимодействия с V2ray TMessagesProj/src/main/java/org/telegram/ui/V2rayConfigFragment.java с редактируемым полем  "Запрос 1" request1Row должно происходить следующее: 

1. При тапе ( коротком ) на поле request1Row "Запрос 1" :
 1.1. Если поле request1Row чем-то заполнено, то поле доступно  в режим текстового редактирования поля request1Row вручную пользователем.
 1.2. Если поле request1Row пусто, то  должно открывается окно ChatActivity переписки с ботом (А НЕ окно выбора чата DialogsActivity) . Имя бота беретс  из ранее заполненного поля `subscription1Row`.

-1.3.  При возврате из ChatActivity  происходит автозаполнение поля request1Row  из буфера обмена (выбранным в окне ChatActivity переписки с ботом  ) 
1.4. Если стереть вручную путем редактирования поле request1Row , затем выйти из панели настроек ,и потом зайти обратно , то поскольку поле request1Row пусто, то при тапе на него должно открывается окно ChatActivity переписки с ботом (по п. 1.2.)   

1.5. При тапе на уже ранее заполненное поле request1Row ничего открывать не надо, а пользователю должны быть доступны все стандартные ф-ции редактирования текстовых редактируемых полей (с стандартным всплывающим при долгом тапе мень "выделить, выбрать все, удалить ")
1.6. редактируемое поле request1Row должно находиться в 1й строке (на 1м вертикальном уровне ) с заголовком "Запрос 1", но правее его (не загораживая заголовок собой)

2. Заголовки редактируемых и обновляемых полей :
 
При ЛЮБЫХ непонятках, сомнениях и отходе от задач speciification.md сначала останавливайся и уточняй - задавай уточняющие вопросы!!!

fix build errors
